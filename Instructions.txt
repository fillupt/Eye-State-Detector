Python 3.12 (e.g. 3.12.10)
Add to PATH during install.

Open terminal (e.g. PowerShell)
	py -3.12 -m venv eye-env
	eye-env\Scripts\activate

Should activate env
Install dependencies:
	pip install opencv-python mediapipe numpy

Now can run:
python Eye_State_Detector.py